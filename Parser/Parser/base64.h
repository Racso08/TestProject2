#ifndef BASE64_H
#define BASE64_H

#include <QByteArray>
#include <QTextStream>
#include <QFile>
#include <QDebug>

class base64
{


      public:
          base64() {} // Dummy for now because it is not the point

};

#endif // BASE64_H
